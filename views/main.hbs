<!doctype html>

<head>
    <title>{{title}}</title>
</head>

<body>
    <h1>{{message}}</h1>
    <ul>
        {{#each products}}
        <li>{{this}}</li>
        {{/each}}
    </ul>
</body>

</html>